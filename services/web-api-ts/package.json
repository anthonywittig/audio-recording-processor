{
  "name": "web-api-ts",
  "version": "0.1.0",
  "private": true,
  "description": "Lambda API for the web app: presigns ingest-bucket uploads and lists recordings + pipeline artifacts",
  "scripts": {
    "build": "./build.sh",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.632.0",
    "@aws-sdk/client-secrets-manager": "^3.632.0",
    "@aws-sdk/s3-request-presigner": "^3.632.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "@types/node": "^20.14.0",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.0"
  }
}
